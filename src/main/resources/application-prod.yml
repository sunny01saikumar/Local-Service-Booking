spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    show-sql: false
app:
  security:
    jwt-secret: ${JWT_SECRET}
logging:
  level:
    root: INFO
    com.sai.geoLocation: INFO
